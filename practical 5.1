#include<iostream>
using namespace std;
  
  struct collage_Info{
      string collage_name ;
      int collage_code ;
      string dept ;
      int intake ;
  };
  
int main()
{
    collage_Info c1 ;
    
      c1. collage_name = "Government polytechnic junagadh" ;
      c1. collage_code = 619 ;
      c1. dept = "computer";
      c1. intake = 79 ;
     
    
      cout << "collage_name : " <<  c1. collage_name << endl;
      cout << "collage_code : " <<  c1. collage_code << endl;
      cout << "dept : " <<  c1. dept << endl;
      cout << "intake  : " <<  c1. intake << endl;
     
     
    return 0 ;
}
